apiVersion: v1
kind: Secret
metadata:
  name: {{ include "common.names.fullname" . }}-secret
  labels:
    {{- include "common.labels" . | nindent 4 }}
type: Opaque
data:
  {{ if .Values.mariadb.enabled }}
  database__connection__password: {{ .Values.mariadb.auth.password | b64enc }}
  {{ end }}
